"""focus_coach_native — Python prototype brain.

A passive, always-on focus monitor: periodically capture the screen, classify
"is the user working / focused?", append a one-line activity readout, and delete
the capture immediately. Nothing leaves the machine.

This package holds the prototype classification logic that gets nailed here
before being ported to the native (Swift / Tauri) shell.
"""

__version__ = "0.0.1"
